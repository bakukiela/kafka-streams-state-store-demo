# kafka-streams-state-store-demo
Learning project for Apache Kafka showing basic producer–consumer flow using Confluent Kafka Python client. Includes sending JSON messages (orders) to a topic and consuming them in real time.
